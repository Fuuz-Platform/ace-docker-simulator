FROM node:22-bookworm-slim
WORKDIR /app
COPY simulator/package.json ./
RUN npm install --omit=dev --no-audit --no-fund
COPY simulator/opcua-server.js simulator/handshake.js ./
ENV NODE_ENV=production OPCUA_PORT=4840
EXPOSE 4840
CMD ["node", "opcua-server.js"]
